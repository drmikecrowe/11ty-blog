User-agent: *
{{/* robotstxt.org - if we are in production, allow access, else deny */ -}}
{{ if hugo.IsProduction -}}
Allow: /

# Prevent crawling of duplicate pagination page 1 (same as /posts/)
Disallow: /posts/page/1/
Disallow: /tags/*/page/1/
Disallow: /categories/*/page/1/

Sitemap: {{ "/sitemap.xml" | urls.AbsURL }}
{{ else -}}
Disallow: /
{{ end -}}
